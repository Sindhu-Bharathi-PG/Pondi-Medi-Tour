const fastifyPlugin = require('fastify-plugin');

async function profileRoutes(fastify, options) {
  // Placeholder routes for profile module
  // Add routes here as needed
}

module.exports = fastifyPlugin(profileRoutes);

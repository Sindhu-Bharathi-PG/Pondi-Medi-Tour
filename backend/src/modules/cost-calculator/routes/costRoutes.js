const fastifyPlugin = require('fastify-plugin');

async function costRoutes(fastify, options) {
  // Placeholder routes for cost calculator module
  // Add routes here as needed
}

module.exports = fastifyPlugin(costRoutes);

const fastifyPlugin = require('fastify-plugin');

async function wellnessRoutes(fastify, options) {
  // Placeholder routes for wellness module
  // Add routes here as needed
}

module.exports = fastifyPlugin(wellnessRoutes);

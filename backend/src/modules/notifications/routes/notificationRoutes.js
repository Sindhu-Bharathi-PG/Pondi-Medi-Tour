const fastifyPlugin = require('fastify-plugin');

async function notificationRoutes(fastify, options) {
  // Placeholder routes for notification module
  // Add routes here as needed
}

module.exports = fastifyPlugin(notificationRoutes);

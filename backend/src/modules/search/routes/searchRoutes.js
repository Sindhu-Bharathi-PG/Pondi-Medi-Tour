const fastifyPlugin = require('fastify-plugin');

async function searchRoutes(fastify, options) {
  // Placeholder routes for search module
  // Add routes here as needed
}

module.exports = fastifyPlugin(searchRoutes);

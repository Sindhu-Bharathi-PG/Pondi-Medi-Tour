const fastifyPlugin = require('fastify-plugin');

async function adminRoutes(fastify, options) {
  // Placeholder routes for admin module
  // Add routes here as needed
}

module.exports = fastifyPlugin(adminRoutes);

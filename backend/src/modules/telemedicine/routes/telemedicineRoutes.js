const fastifyPlugin = require('fastify-plugin');

async function telemedicineRoutes(fastify, options) {
  // Placeholder routes for telemedicine module
  // Add routes here as needed
}

module.exports = fastifyPlugin(telemedicineRoutes);

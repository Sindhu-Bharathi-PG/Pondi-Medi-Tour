const fastifyPlugin = require('fastify-plugin');

async function visaRoutes(fastify, options) {
  // Placeholder routes for visa module
  // Add routes here as needed
}

module.exports = fastifyPlugin(visaRoutes);

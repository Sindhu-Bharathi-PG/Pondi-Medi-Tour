const fastifyPlugin = require('fastify-plugin');

async function paymentRoutes(fastify, options) {
  // Placeholder routes for payment module
  // Add routes here as needed
}

module.exports = fastifyPlugin(paymentRoutes);

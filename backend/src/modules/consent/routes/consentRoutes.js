const fastifyPlugin = require('fastify-plugin');

async function consentRoutes(fastify, options) {
  // Placeholder routes for consent module
  // Add routes here as needed
}

module.exports = fastifyPlugin(consentRoutes);

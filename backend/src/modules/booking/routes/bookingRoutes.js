const fastifyPlugin = require('fastify-plugin');

async function bookingRoutes(fastify, options) {
  // Placeholder routes for booking module
  // Add routes here as needed
}

module.exports = fastifyPlugin(bookingRoutes);
